public interface constants {
    int gridRows = 4;
    int gridCols = 4;
    int numberOfBoards = 4; // Example value, adjust as needed
    int numberOfThreads = 4; // Example value, adjust as needed
    int sectionSize = numberOfBoards / numberOfThreads; // Size of each thread's section
    // Add other constants as needed
}